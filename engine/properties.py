# Mitsuba-specific render properties, UI panels, and dynamic plugin property
# classes have been removed.  The addon now uses Cycles with GPU compute
# exclusively; the mitsuba Python package is still used for XML import/export.
